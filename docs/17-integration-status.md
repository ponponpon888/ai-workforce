# 修正の統合状況（2026-09-09）

PR #9 を土台に、#10〜#15 の変更をまとめた検証用ブランチです。
各 PR の設定・インストーラ・フックを一緒に使える状態にし、CI のテスト追加が競合した箇所を整理しています。
リリース完了や実環境での動作確認済みを意味しません。

## 取り込んだ変更

| PR | 取り込んだコミット | 内容 |
|---|---|---|
| #9 | `58fc082357c4cc50a05e9a41172b36ec455713c6` | 落とし穴レコード、SQL ファイル検査、CI 構成 |
| #10 | `f116370405ad54b7c39b187e5eddd2fa0313f51f` | 設定形式の修正、読み取り専用 doctor |
| #11 | `eb119084d4343abf9afd6c62a4da97dfb4db8e89` | SQL の文境界、完全一致・一度限りの承認 |
| #12 | `5191dd7ef37a7d5274b5d6ba4ff5faffa7ee31c2` | 落とし穴の静的検査 |
| #13 | `db60016c45ea7d76e1e2107d26772c200e32babb` | インストーラ事前検査、バックアップ保持、パス処理 |
| #14 | `56f4815f73f8d46694bda002ec956aaa3791dced` | 文書内の未完了マーカー検査 |
| #15 | `52b2f5b4f58365e055b4ff70d7f03ed838c2c14b` | 単体試行、既存設定への手動取り込み、復元手順 |

元 PR の後続変更は自動で取り込まれません。更新する際はこの表のコミットとの差分を確認してください。
CI の既存ジョブ名と OS 選択を保ち、それぞれの追加テスト・ファイル存在確認・doctor を残しました。
新しいジョブは追加していません。

## 現在の統合版の検証結果

検証対象コード: `81de706a1ac2970f2082e35021663097d5ceebb7`。
Linux / Node v24.19.0 で全11スイートを再実行し、合計346件成功・失敗0件でした。
この記録の更新は文書のみです。

| スクリプト（`kit/scripts/`） | 成功数 |
|---|---:|
| `test-check-doc-todos.mjs` | 13 |
| `test-doctor.mjs` | 27 |
| `test-guard-secrets.mjs` | 51 |
| `test-guard-sql.mjs` | 44 |
| `test-install-backups.mjs` | 27 |
| `test-installed-approval.mjs` | 1 |
| `test-lint-pitfalls.mjs` | 38 |
| `test-pull-all.mjs` | 67 |
| `test-sql-boundaries.mjs` | 18 |
| `test-validate-pitfalls.mjs` | 51 |
| `test-verify-installers.mjs` | 9 |

前回の静的検証（`8961b417`）では、テンプレートの doctor は `static-pass`。落とし穴の静的検査は5件成功・違反0・不明0で、7レコードは検査対象外です。
12レコードの形式検証、生成インデックスと保存済みデータの一致、文書の未完了マーカー0件を確認しました。
静的検査は実行環境全体の安全性を証明するものではありません。

## 統合後に加えた改善

- 配置された承認スクリプトと設定中の SQL フックを接続し、完全一致・一度限りの承認を検査。
- ローカル HTTP 401 と実際の Git で認証失敗を検査。非対話設定の受け渡しも確認。
- 作業ブランチの取得失敗を `fail/fetch`、ローカル更新失敗を `fail/update` として報告。履歴の分岐は `skip/diverged`。
- 壊れたインデックスなどの状態検査エラーを `fail/status` として報告し、そのリポジトリの更新を停止。
- Node 版の未知・重複・値不足のオプション、不正な保持日数・リポジトリ指定を処理前に拒否。
- 更新先の存在とディレクトリ種別をログ作成前に検査。
- Node 版の dry-run ではログの作成・追記・削除を行わず、更新予定だけを表示。
- 検証ランナーに `--suite pull-all` を追加し、利用できないランタイムを未検証として報告。

詳細は [自動更新](04-multi-project.md)、[認証検査](18-pull-auth-check.md)、[インストーラ検証](12-installer-backups.md) を参照してください。
テストは一時ファイル・検査用リポジトリを使用し、本番 DB やユーザーの作業リポジトリには接続しません。

## 残っている確認

| 対象 | 状況 |
|---|---|
| Linux / Node | 上記346件成功 |
| PowerShell 7 / Windows PowerShell 5.1 | 今回の統合コードは未実行。自動更新スイートは55項目の構成 |
| macOS / Windows の Node | 今回の統合コードは未実行 |
| GitHub Actions | run `34415839194` は18ジョブ・実行ステップ0件で失敗 |
| Claude Code 本体 | 設定受理・設定優先順位・実際のフック接続は未検証 |
| 素の Windows 環境 | 新規導入からの動作確認が未完了 |
| 実プロジェクトの DB ロール | 未検証。対象 DB に変更は加えていない |

CI 停止の解消は確認できていません。ローカル成功を CI 成功として扱いません。
過去の文書にある Windows 実測や CI 成功の記述も、この統合版の実行結果とは区別してください。
権限のパス記法・コマンド変種など、その他の未完了事項は [ロードマップ](../ROADMAP.md)に残しています。

## 手元での次の検証

リポジトリのルートから実行します。各コマンドの結果を確認して次へ進んでください。

```powershell
node kit/scripts/verify-installers.mjs --json
node kit/scripts/verify-installers.mjs --suite pull-all --json
```

0は全対象成功、1はテスト失敗あり、2は未検証の対象ありです。
これら2コマンドはインストーラと自動更新の検証であり、SQL ガードなど全スイートをまとめた検証ではありません。
PowerShell が利用できない Linux での実行は、Node 成功・PowerShell 未検証で終了コード2を確認済みです。

以下は各変更時点の履歴です。現在の全体検証は上記346件を参照してください。

## 検証後の追加: 名前指定した更新先の確認

Node の `--repos` が存在しない指定を黙って除外する問題を修正しました。
指定先をすべて事前確認し、不正な対象があれば残りの更新も始めず終了コード1で停止します。
変更後の pull-all スイートは Linux / Node で51件成功（4件追加）。
323件は当時の全体検証で、今回の再実行は変更した pull-all が対象です。
PowerShell の `-Repos` は今回変更していません。

## 追加: PowerShell の名前指定も事前確認

PowerShell の `-Repos` に全指定先の事前確認を追加しました。
存在しない指定先・通常フォルダ・壊れた `.git` の拒否と、正しい指定での更新を共通スイートへ移しました。
Node の51項目は成功。PowerShell 対象は36項目の構成ですが、今回の実行は未検証です。

## 追加: PowerShell の更新予定表示

`pull-all.ps1 -DryRun` を追加しました。取得とブランチ更新を行わず、ログディレクトリの作成・追記・古いログの削除も抑止します。
既存の dry-run 検査3件を Node / PowerShell 共通にしました。
変更後の Linux / Node は51件成功。PowerShell 対象は39項目の構成で、ランタイムがないため未実行です。
予定表示はローカル状態に基づき、リモートの到達性や fast-forward の成否を保証しません。

## 追加: 検証対象の誤入力を拒否

インストーラ・自動更新のテストが未知の `--target` を Node として実行する問題を修正しました。
共通の引数検査で未知・重複・値不足の指定と、Node 対象での `--pwsh` を開始前に拒否します。
変更した3スイートを Linux / Node で確認し、インストーラ27件・自動更新51件・検証ランナー9件が成功しました。
PowerShell 本体の実行検証は引き続き未完了です。

## 追加: revert / sequencer の途中を保護

自動更新の操作途中判定に `REVERT_HEAD` と `sequencer` を追加しました（Node / PowerShell 共通）。
差分のない作業ツリーでも取得前に `skip/in-progress` として停止し、操作状態を保持します。
実際の revert 競合を使うケースと sequencer 状態を配置するケースで6項目を追加しました。
修正前は5項目が失敗し、修正後の Linux / Node は57件すべて成功しました。
PowerShell 対象は45項目の構成ですが、ランタイム不在のため未実行です。

## 追加: 表示設定で隠れた変更を検出

Node / PowerShell 両版の Git 状態検査に未追跡ファイル・サブモジュール変更の検出オプションを明示しました。
変更を非表示にする設定でも、自動更新は取得前に `skip/dirty` として停止します。
実リポジトリと実サブモジュールによる回帰検査6項目を追加し、修正前は6失敗、修正後の Linux / Node は63件成功しました。
PowerShell 対象は51項目の構成ですが、ランタイム不在のため未実行です。

## 追加: ログ保存失敗を明示して停止

Node / PowerShell 両版でログ作成・追記エラーを終了コード1として扱い、以降の更新を停止するよう変更しました。
ログ保存先がファイルの場合と、親パスがファイルの場合に、取得・ブランチ更新を始めない回帰検査4項目を追加しました。
Linux / Node は67件成功。PowerShell 対象は55項目の構成ですが、実行環境がなく未検証です。
更新後にログが書けなくなった場合、完了済みの更新は保持されます。

## 追加: PowerShell の不正なログ保持日数を拒否

`-LogRetentionDays` の0・負数を、ログ処理やリポジトリ更新より前に終了コード2で拒否します。
古いログを保持したまま更新を止める共通検査2項目を追加しました。
変更後の Linux / Node は69件成功。PowerShell 対象は57項目の構成で、実行は未検証です。
上記346件は追加前の全体検証で、今回は自動更新スイートのみ再実行しました。

## 追加: ブランチ取得失敗時の更新を停止

Node / PowerShell 両版で現在のブランチ取得結果を検査し、失敗時は `fail/branch` としてその対象の更新を止めます。
実際のコミットのないリポジトリで、取得やブランチ作成に進まない共通検査3項目を追加しました。
修正前は2項目失敗、変更後の Linux / Node は72件成功。PowerShell 対象は60項目の構成で、実行は未検証です。

## 追加: 検証ランナーで実行環境を選択

`verify-installers.mjs --target` を追加し、Node・PowerShell 7・Windows PowerShell 5.1 のいずれか、または全対象を選べます。
結果には選択範囲を明記し、終了コード0は選択した対象についてのみ成功を意味します。
検証ランナー13件成功。実スイートも `--suite pull-all --target node` で成功を確認しました。
Linux で Windows PowerShell 5.1 を指定した場合は未検証・終了コード2でした。PowerShell 本体は未実行です。

## 追加: 秘密ファイルガードの Windows パス対応

`guard-secrets` の Node / PowerShell 両版で、パス区切りを統一してから秘密ファイルを判定します。
Windows形式の secrets 配下の設定・.ssh 配下の読み取りを検出し、文書とソースコードの例外は維持しました。
修正前に2ケースの検出漏れを再現。修正後の秘密ファイルガードは Linux / Node で55件成功。PowerShell 本体は未実行です。

## 追加: 拡張子付き実行ファイルの秘密読み取り検出

Node / PowerShell 両版で、読取コマンド判定時に `.exe` 接尾辞を正規化します。
Git のサブコマンド検査にも適用し、show/diff と add の区別を維持しました。
修正前に4ケースの検出漏れを再現し、修正後の Linux / Node は61件成功。PowerShell 本体は未実行です。

## 追加: 公開テンプレート例外の判定範囲

親フォルダ名に含まれた example / sample を理由に、本物の `.env` が許可される問題を修正しました。
Node / PowerShell 両版で公開テンプレート例外をファイル名だけに適用します。
Windows形式を含む検出漏れ2件を修正前に再現し、修正後の Linux / Node は65件成功。PowerShell 本体は未実行です。

## 追加: 秘密ファイルガードを検証ランナーへ接続

`--suite guard-secrets` を追加し、実行環境の選択・JSONレポート・未検証判定を秘密ファイルガードにも適用しました。
直接のテスト実行も共通の引数検査を使い、対象名の誤入力による Node への暗黙の切り替えを拒否します。
検証ランナー15件成功。実ガードも Node 指定で成功し、Linux で Windows 指定時は未検証・終了コード2でした。
PowerShell 本体は未実行です。
