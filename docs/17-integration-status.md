# 修正の統合状況（2026-09-09）

PR #9 を土台に、#10〜#15 の変更をまとめた検証用ブランチです。
各 PR の設定・インストーラ・フックを一緒に使える状態にし、CI のテスト追加が競合した箇所を整理しています。
リリース完了や実環境での動作確認済みを意味しません。

## 取り込んだ変更

| PR | 取り込んだコミット | 内容 |
|---|---|---|
| #9 | `58fc082357c4cc50a05e9a41172b36ec455713c6` | 落とし穴レコード、SQL ファイル検査、CI 構成 |
| #10 | `f116370405ad54b7c39b187e5eddd2fa0313f51f` | 設定形式の修正、読み取り専用 doctor |
| #11 | `eb119084d4343abf9afd6c62a4da97dfb4db8e89` | SQL の文境界、完全一致・一度限りの承認 |
| #12 | `5191dd7ef37a7d5274b5d6ba4ff5faffa7ee31c2` | 落とし穴の静的検査 |
| #13 | `db60016c45ea7d76e1e2107d26772c200e32babb` | インストーラ事前検査、バックアップ保持、パス処理 |
| #14 | `56f4815f73f8d46694bda002ec956aaa3791dced` | 文書内の未完了マーカー検査 |
| #15 | `52b2f5b4f58365e055b4ff70d7f03ed838c2c14b` | 単体試行、既存設定への手動取り込み、復元手順 |

元 PR の後続変更は自動で取り込まれません。更新する際はこの表のコミットとの差分を確認してください。
CI の既存ジョブ名と OS 選択を保ち、それぞれの追加テスト・ファイル存在確認・doctor を残しました。
新しいジョブは追加していません。

## この組み合わせで確認したこと

Linux / Node v24.19.0 で、以下の11スイート計297件が成功しました。

| スクリプト（`kit/scripts/`） | 成功数 |
|---|---:|
| `test-doctor.mjs` | 27 |
| `test-lint-pitfalls.mjs` | 38 |
| `test-install-backups.mjs` | 27 |
| `test-verify-installers.mjs` | 4 |
| `test-guard-sql.mjs` | 44 |
| `test-sql-boundaries.mjs` | 18 |
| `test-guard-secrets.mjs` | 51 |
| `test-pull-all.mjs` | 23 |
| `test-validate-pitfalls.mjs` | 51 |
| `test-check-doc-todos.mjs` | 13 |
| `test-installed-approval.mjs` | 1 |

最後のテストは今回追加しました。一時ディレクトリへインストールし、doctor を通し、
生成された設定の SQL フックコマンドと配置された承認スクリプトを使います。
未承認の拒否、文字列リテラル中の空白変更の拒否、元の文の一度だけの許可、再使用の拒否を照合します。
SQL はフックへの入力だけで、DB 接続・SQL 実行は行いません。

```powershell
node kit/scripts/test-installed-approval.mjs
```

テンプレートの doctor は `static-pass`、落とし穴の静的検査は5件成功・7レコード検査対象外、文書のマーカーは0件でした。
静的検査は実行環境全体の安全性を保証しません。

## 残っている確認

- Windows PowerShell 5.1 / PowerShell 7、macOS と CI の実行結果。この環境には PowerShell がありません。
- 素の Windows 環境での導入、Claude Code 本体の設定受理・設定優先順位・フック接続。
- 実プロジェクトの読み取り専用 DB ロール検証。対象 DB には変更を加えていません。
- 権限のパス記法・コマンド変種の実測など、[ロードマップ](../ROADMAP.md)の未完了項目。

CI は直近までジョブがステップ実行前に失敗しており、この統合のローカル成功で解消したとは扱いません。
元の文書にある過去の Windows 実測や CI 成功の記述も、この統合版の検証結果とは区別してください。

## 追加の認証検査

`test-pull-all.mjs` の文字列検索1項目を実際の Git 認証検査へ置き換えました。
変更後の同スイートは Linux / Node で23項目成功。統合版のテスト総数は297のままです。
2種類の認証設定を個別に外した負の対照も失敗を検出しました。
詳しい検査範囲と未検証事項は [認証検査](18-pull-auth-check.md) を参照してください。

## 作業ブランチの認証失敗の修正

作業ブランチの認証失敗が `skip/diverged`・終了コード0になる不具合を再現し、
Node / PowerShell 両実装でリモート取得失敗とローカル更新失敗を分離しました。
変更後の pull-all スイートは Linux / Node で27項目成功（従来23項目から4項目追加）。
上の297件は初回統合時の実行結果です。今回の再実行は変更の影響する pull-all スイートが対象です。
PowerShell 版は未実行です。

## Node 版の引数検査

自動更新の未知のオプション・値不足・重複指定、不正な保持日数とリポジトリ指定を、
書き込み前に終了コード2で拒否するようにしました。
Node 固有の12検査を追加し、変更後の pull-all スイートは Linux / Node で39項目成功。
PowerShell 対象では従来の27項目を実行する構成です。今回は PowerShell 未実行です。

## 存在しない更新先の検出

ルートの検査より先にログ用ディレクトリを作り、存在しないルートを作成してしまう問題を修正しました。
Node / PowerShell 両実装でルートの存在・ディレクトリ種別を先に確認します。
欠損パスと通常ファイルの2ケースを追加し、Linux / Node で変更後41件成功。
PowerShell 対象は29項目の構成になりましたが、今回は未実行です。
